[project]
name = "synthetic-pdf-generator"
version = "0.1.0"
description = "Generate synthetic W-2 and pay stub PDFs for Loan Officer Copilot demos"
requires-python = ">=3.11"
dependencies = [
  "reportlab>=4.0.0",
]
